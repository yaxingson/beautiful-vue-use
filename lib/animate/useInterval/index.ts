import { onMounted, onBeforeUnmount, toValue } from 'vue'
import type { MaybeRefOrGetter } from 'vue'

function useInterval(callback:Function, delay?:MaybeRefOrGetter<number|null>) {
  

}

export default useInterval
